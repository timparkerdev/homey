<div class="bg-white py-24 sm:py-32">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-2xl lg:mx-0">
      <h2 class="text-pretty text-4xl font-semibold tracking-tight text-gray-900 sm:text-5xl">👋 hi</h2>
      <p class="mt-6 text-lg/8 text-gray-600">.</p>
    </div>
  </div>
</div>

<div class="bg-white">
  <div class="mx-auto max-w-7xl px-6 py-24 sm:pt-32 lg:px-8 lg:py-40">
    <div class="lg:grid lg:grid-cols-12 lg:gap-8">
      <div class="lg:col-span-5">
        <h2 class="text-pretty text-3xl font-semibold tracking-tight text-gray-900 sm:text-4xl">Brief</h2>
        <p class="mt-4 text-pretty text-base/7 text-gray-600">Presented here are some questions I would ask about the brief. For most of these, I've assumed an agile approach to delivery, whereby the focus is on shipping a usable MVP, which can be expanded on easily 😊.</p>
      </div>
      <div class="mt-10 lg:col-span-7 lg:mt-0">
        <dl class="space-y-10">
          <% assumptions_helper.map do |assumption| %>
            <div>
              <dt class="text-base/7 font-semibold text-gray-900"><%= assumption.first %></dt>
              <dd class="mt-2 text-base/7 text-gray-600"><%= assumption.second %></dd>
            </div>
          <% end %>
        </dl>
      </div>
    </div>
  </div>
</div>
